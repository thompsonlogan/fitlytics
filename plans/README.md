# Implementation Plans

Generated by the `improve` skill on 2026-06-14, planned against commit `cb2af4b`.
Execute in the order below unless dependencies say otherwise. Each executor: read
the plan fully before starting, honor its STOP conditions, run every verification
command, and update your row in the table when done.

All plans were written for an executor with **zero context** from the audit
session. Everything needed is inlined in each plan file.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | CI pipeline (GitHub Actions) | P1 | S | 011, 012 | DONE — **merged with 011+012 into commit `4b5ccda` on `improve-1`** (merge-as-a-set satisfied). Reconcile-verified 2026-06-14 on HEAD `4b5ccda`: `.github/workflows/ci.yml` present and correct; frontend gate green (see 011/012); backend untouched since the integration-verified state. |
| 002 | Backend auth test coverage (verifier, session, principal) | P1 | M | — | TODO |
| 003 | Re-verify content-type on video finalize | P2 | S | — | TODO |
| 004 | Fix N+1 in batch set-log update | P2 | M | — | TODO |
| 005 | Frontend critical-hook tests | P2 | M | — | TODO — reconcile 2026-06-14: re-based to `4b5ccda`. `use-session.ts` changed only in out-of-scope `useLogSetBatch`; finding still valid, no meaningful drift. |
| 006 | Wire dead nav buttons to routes (branded 404) | P2 | S | — | TODO |
| 007 | Consolidate sentinel errors into `apierr` | P3 | S | — | TODO |
| 008 | Code-split the landing route | P3 | S | — | TODO |
| 009 | Replace hand-rolled batch fetch with generated client | P3 | S | — | TODO |
| 010 | Fix stale README dev-proxy note | P3 | S | — | TODO — reconcile 2026-06-14: re-based to `4b5ccda`. Stale note still at `README.md:106`; finding still valid. |
| 011 | Fix 3 pre-existing frontend failures (lint x2 + test x1) | P1 | S | — | DONE — merged into `4b5ccda` on `improve-1`. Reconcile-verified 2026-06-14 on HEAD `4b5ccda`: `pnpm lint` exit 0, `pnpm test` 57/57 pass. `pnpm build` criterion struck (build breakage was pre-existing, tracked as 012). |
| 012 | Fix `pnpm build` (`tsc -b`) — 143 pre-existing type errors + no-op typecheck | P1 | M | — | DONE — merged into `4b5ccda` on `improve-1`. Reconcile-verified 2026-06-14 on HEAD `4b5ccda`: `pnpm typecheck` (`tsc -b`) → 0 errors. |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Dependency notes

- No hard ordering dependencies between plans — each is self-contained and can be
  executed independently.
- **Soft ordering**: land **001 (CI)** and **002 (auth tests)** first. CI becomes
  more valuable once it has the auth tests to enforce, and CI then guards every
  later plan's PR.
- **HARD ordering (discovered 2026-06-14 during 001 execution)**: **011 must land
  before 001 can be marked DONE.** The 001 workflow file is written and correct, but
  the frontend job runs `pnpm lint` + `pnpm test`, both of which fail on `master`
  today for reasons unrelated to CI: (1) `src/components/workout/set-state-cell.tsx`
  exports `SetState`/`CYCLE_NEXT` alongside a component (`react-refresh/only-export-components`);
  (2) `src/routes/today.tsx:29` `useMemo` deps narrower than what the body reads
  (`react-hooks/preserve-manual-memoization`); (3) `src/hooks/use-set-videos.ts:15`
  reads `import.meta.env.VITE_ALLOWED_VIDEO_TYPES.split(...)` which is undefined under
  Vitest, crashing `use-set-videos.test.tsx`. Merging 001 before 011 would create a
  permanently-red required check and block branch protection.
- **009 requires a running backend + Docker** to regenerate the API client. It is
  the only plan that cannot be completed in a pure offline worktree — see its STOP
  conditions. Sequence it whenever a full local stack is available.

## Findings considered and REJECTED (so they aren't re-audited)

- **"Session-state recompute runs outside its transaction"** — FALSE. `recomputeSessionState`
  is called at `backend/internal/sessions/repository.go:440`, *inside* the `Transaction`
  closure (`q := query.Use(tx)`). Not a bug.
- **"Credentials committed in `.env`"** — FALSE. `backend/.env` is gitignored and never
  tracked; only `.env.example` is in git. `git log` shows no `.env` history.
- **"AUTH_BYPASS not gated to dev"** — FALSE. `backend/cmd/api/main.go:74-77` hard-exits
  (`os.Exit(1)`) if `AUTH_BYPASS_USER_ID` is set while `APP_ENV=production`.
- **"Empty `IN()` clause corrupts session-state recompute"** — theoretical. gorm/gen
  `In()` with an empty slice yields a no-match, and the `total == 0` switch arm
  (`repository.go:352`) handles the empty case safely.
- **"Per-day video quota is timezone-buggy"** — by design. `repository.go:72` enforces a
  rolling 24h window, not a calendar day; `MAX_VIDEOS_PER_DAY` is documented as such.
- **"MarkReady/MarkFailed lack ownership checks"** — defense-in-depth only. Both are
  called solely from `Finalize`, which verifies ownership via `GetOwned(videoID, ownerID)`
  first (`videos/service.go:116`). No reachable IDOR. Not worth a parameter-threading change.
- **"Generic mapper abstraction across aggregates"** — not worth doing. The three mapper
  trees are similar but the shared-generic refactor is L effort for near-zero benefit and
  risks subtle nil/capacity differences. Revisit only if a 4th aggregate is added.
- **"videos package is untested"** — FALSE. `videos/{service,handler,repository}_test.go`
  all exist and are thorough. (`storage/r2.go` is untested, but it is a thin wrapper over
  the AWS SDK and needs an S3 mock; deferred as low-leverage.)

## Notes for the human owner

- The repo has **no CI today** (`.github/` is absent) but merges via PRs (#9–#14).
  Plan 001 closes that gap.
- Backend tests use Go stdlib + hand-written fakes + `go-sqlmock` (no real DB needed).
  Frontend tests use Vitest + Testing Library (jsdom).
- Plans 007–010 are low-leverage cleanups bundled at the user's request; 010 fixes a
  doc that is actively wrong, which is the most worthwhile of the four.
- **011 was added 2026-06-14 during 001 execution.** Executing 001 surfaced that the
  frontend suite is red on `master` (2 lint errors + 1 test crash) for reasons the
  original audit missed. 011 fixes them; 001's workflow file is already written and
  correct and just waits on 011 to go green. The test crash only manifests where
  `frontend/.env` is absent (CI, fresh worktree) — `.env` is git-ignored — which is
  why it wasn't visible in normal local dev. **011 is now DONE.**
- **012 was added 2026-06-14 during 011 execution.** Executing 011 surfaced a fourth,
  larger pre-existing failure: `pnpm build` (`tsc -b`, build mode) fails with **143
  type errors** on clean `master` (verified at `cb2af4b` in both the main tree and a
  fresh worktree; 011 introduced none of them). Root cause: `pnpm typecheck`
  (`tsc --noEmit`) reads the root `tsconfig.json`, which has `"files": []` and only
  project *references* — non-build `tsc` ignores references, so **typecheck compiles
  zero files and passes meaninglessly**, hiding the errors that `tsc -b` (build mode)
  catches. The 143 errors split across: ~110 in the generated OpenAPI client
  (`src/services/generated/**`), real app-code type errors (`day-board.tsx`,
  `use-session.ts`, `video-upload-dialog.tsx` — `string | undefined` passed where
  `string` required), and stale test fixtures (`program-data.test.ts`,
  `use-workout-program.test.tsx`). 012 needs scoping before a plan is written — see
  "Open question" below. **001 cannot go fully green until 012 lands** (its frontend
  job runs `pnpm build`).

## 012 resolution (2026-06-14) — RESOLVED

Maintainer chose "fix the build properly." Done: dropped three codegen-hostile
tsconfig flags (`noUnusedLocals`, `noUnusedParameters`, `erasableSyntaxOnly` —
redundant with ESLint for app code, niche for a bundled SPA) to clear all 128
generated-client errors without weakening `strict` or ESLint's unused-var rule;
fixed the 15 genuine app/test type errors; and made `pnpm typecheck` a real check
(`tsc -b` instead of the no-op `tsc --noEmit`). Verified `tsc -b` → 0 errors.

## Merge guidance for 001 / 011 / 012 — RESOLVED (2026-06-14)

All three landed **together** in commit `4b5ccda` on branch `improve-1`, satisfying
the merge-as-a-set requirement (001's CI would have been red without 011+012). The
separate worktree branches from execution are no longer relevant.

## Reconcile report — 2026-06-14 (HEAD `4b5ccda`, branch `improve-1`)

Re-ran since the plans were written against `cb2af4b`. Only `.github/`, `frontend/`,
and `plans/` changed between `cb2af4b` and `4b5ccda`; no backend files moved.

**DONE — verified on HEAD:**
- **001** — `.github/workflows/ci.yml` present and correct.
- **011** — `pnpm lint` exit 0, `pnpm test` 57/57 pass.
- **012** — `pnpm typecheck` (`tsc -b`) → 0 errors.
- (Combined: typecheck + lint + test all green on HEAD, so the CI frontend job passes.)

**TODO — drift-checked, all still executable:**
- **005, 010** — in-scope/evidence files shifted slightly; findings re-confirmed valid,
  `Planned at` re-based to `4b5ccda` (see each plan's reconcile note). No excerpt content
  changed meaningfully.
- **002, 003, 004, 006, 007, 008, 009** — no in-scope files changed since `cb2af4b`; no
  drift. Left at their original `Planned at` (their drift checks return clean).

**Rejected this pass:** none.

**Executable right now (no external deps):** 002, 003, 004, 005, 006, 007, 008, 010.
009 still needs a running backend + Docker to regenerate the client (its own STOP
condition).
