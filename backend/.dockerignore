# Local environment — never bake secrets into an image.
.env
.env.*
!.env.example

# Build artifacts (regenerated in-image).
/bin/
/api
/api.exe
/gen
/gen.exe

# Generated OpenAPI package — regenerated by `swag init` in the build stage.
/docs/

# Coverage artifacts.
cover.out
cover.html
